// event listeners

// to change the type of variable from string to number
// parseFloat(variable)

// to change the type from number to string
// variable.toString()

let numberVariable = "";

function handleClickNumber(number) {
  numberVariable = numberVariable + number;
  console.log(numberVariable);
}
